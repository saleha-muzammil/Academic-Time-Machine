#include<iostream>
using namespace std;
float getStandardDeviation (float arr [], int size);
float getAverage (float arr [], int size);
float inputArray (float arr [], int size);
float percentage (float arr [], int size,float average, float std);
float getstd(float arr [], int size);
int main()
{
	int S,N,xi,X,per,ERD,sum,count,Average;
	sum=0;
	S = sqrt(sum(xi-X)^2/N-1);
	Average = sum/N;
		X=Average;
		count = (X-S) < N >(X+S); 
		per =count/N *100;
		cout <<"enter values of xi between 0-99/n";
		cin>>xi;
		cout <<"N= no of values/n";
		cin>>N;
		{ void main ()
		{ Float arr []; int size=10;
		inputArray(data,size);
		printData(data,size);
		float average = getAverage(data,size);
		floatstd = getStandardDeviation(data,size);
		cout<< Average << endl << std << endl;
		cout << per << endl;
		if (per > 65 && per< 75)
			cout << "Good Bell Shape/n";
		else
			cout << "Bad Bell Shape/n";
		}
		}
		system (pause)
			return 0;
}
		

