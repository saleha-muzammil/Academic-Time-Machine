#include<iostream>
#include<string>
using namespace std;
int findSubString (char arr [], int index);
void removeDuplicateWords (char arr []);
int main ()
{
char arr [] ,index,string,elements;
void removeDuplicateWords (char arr []);
if (elements arr repeats);
removeDuplicateWords
else 
(cout<<arr <<endl;);
void main ()
{
	char arr[]=
		removeDuplicateWords(Arr);
	cout << arr << endl;
}
system (pause)
	return=0;
}
