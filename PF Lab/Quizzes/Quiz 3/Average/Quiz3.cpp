#include <iostream>
using namespace std;

int strlen(char array [])
{
     int counte =0;
	 while(array[counter]='\0')
	 {
	 counter++;
	 }
	 return counter;
	 }
	 
	 int main()
	 {
	 char string1[50];
	 char string2[50];
	 int l1strlen(string1);
	 int l2strlen(string2);
	 cout<<"the longer string is:";
	 if(l1>l2)
	 { 
	 cout<<"stringl1"<<"*"<<endl;
	 cout<<"length of longer string is"<<l1<<"*";
	 }
	 else if(l1<l2)
	 {
	 cout<<stringl2<<"*"<<endl;
	 cout<<"length of longer string is"<<l2<<"*";
	 \* if there is any common alphabets in string1 and string2 then show them *\
	 
	 return 0;
	 }
	 
	 
	 
	 
	 
	 
	 
	